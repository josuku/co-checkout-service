export const host = process.env.HOST || 'localhost';
export const port = process.env.PORT || 3001;

export const bill_ms_host = process.env.BILL_MS_HOST || 'localhost';
export const bill_ms_port = process.env.BILL_MS_PORT || 3002;

export const logistic_ms_host = process.env.LOGISTIC_MS_HOST || 'localhost';
export const logistic_ms_port = process.env.LOGISTIC_MS_PORT || 3003;

export const order_ms_host = process.env.ORDER_MS_HOST || 'localhost';
export const order_ms_port = process.env.ORDER_MS_PORT || 3004;